# The Pheasant Reservations

A static npm-runnable version of the reservations and events page.

## Run

```bash
npm start
```

Then open:

```text
http://localhost:3000
```

You can use a different port in PowerShell with:

```powershell
$env:PORT=4000; npm start
```
